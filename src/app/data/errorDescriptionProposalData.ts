import { ProposalValidation } from "../model/proposalValidationModel";

export const ERRORS_PROPOSAL_DESCRIPTION:ProposalValidation = {
  errors:["The description must have between 1 and 1000 characters"],
  state:false,
};