import { ProposalValidation } from "../model/proposalValidationModel";

export const ERRORS_PROPOSAL_LINKEDIN:ProposalValidation = {
  errors:["The link must belong to the LinkedIn domain"],
  state:false,
};